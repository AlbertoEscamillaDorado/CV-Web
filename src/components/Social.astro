---
const { platform, username } = Astro.props;
---

<a href={`https://www.${platform}.com/${username}`}>
    <img src={`public/images/${platform}-logo.png`} alt={`Logo de ${platform}`} />
    {platform}
</a>

<style>
    a {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: white;
        text-decoration: none;
        padding: 0px 50px;
    }
    img {
        width: 50px;
        height: auto;
    }
</style>
